# configs

示例配置目录。

- `connections.example.yaml`：SSH/COM Target，不含真实凭据。
- `settings.example.yaml`：`network.scope=intranet`、三类日志上限、连接空闲回收。
- `recipes/artifact-service.example.yaml`：远端 command 健康检查的 Recipe。

实际数据在 `%LOCALAPPDATA%\LocalAIHub\`，不提交仓库。未知 YAML 字段拒绝解析。
