# 计划：接口约束文档

**状态**：已执行（2026-09-03）  
**产出**：[`contracts/`](contracts/README.md)

过程中的判断与取舍见 git 历史或下文摘要。不再作为施工清单。

摘要：总稿保留架构；字段级契约独立；JSON 顶层扁平；全局 flag 仅 verb 后；deploy 纳入 V1；IPC payload 与 `--json` 同构。
