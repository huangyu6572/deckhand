# internal/log

事件落盘、游标、订阅，以及审计 JSONL（原 output + audit）。

## 动作

- stdout/stderr/串口字节和状态 → 标准事件。
- 每个 operation 从 1 起的事件游标。
- 先追加落盘，再发布；磁盘是唯一事实源。
- 环形缓冲 + 按游标磁盘重放后切实时订阅。
- 脱敏尽力而为；审计哈希链；轮转与保留。

## 边界

不授权、不审批。未持久化事件不得返回 AI。
